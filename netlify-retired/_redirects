# The VHF display moved to GitHub Pages. This site is retired.
#
# There is deliberately no index.html here: Netlify serves a real file in
# preference to a redirect, so an index would shadow the rule below. With the
# directory holding only this file and sw.js, every path falls through to the
# redirect.
#
# sw.js is the exception and must keep resolving — see the note in that file.

/*    https://hambats.github.io/vhf-firetv-display/:splat    301
